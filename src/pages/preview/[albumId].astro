---
import Layout from '@layouts/Layout.astro';
import BookPreview from '@components/UI/edit/preview/Preview'

const albumId = parseInt(Astro.params.albumId)
---

<Layout title="Book Preview">
  <BookPreview albumId={albumId} client:visible/>
</Layout>
